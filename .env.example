# API base URL – must point to ib-backend (e.g. C:\xampp\htdocs\ib-backend)
# No trailing slash; the app will call /smart-apply, /api/auth, etc. under this base.
VITE_API_URL=http://localhost:5000/api
